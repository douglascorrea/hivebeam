defmodule HivebeamLiveview do
  @moduledoc """
  Optional local-only Phoenix LiveView addon for Hivebeam.

  This addon intentionally lives outside core installation flow. It consumes
  shared UI state/events from `Hivebeam.UI.SessionModel`.
  """
end
