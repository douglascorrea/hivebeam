#!/usr/bin/env sh

# Gateway defaults for Hivebeam release runtime
export HIVEBEAM_GATEWAY_BIND="${HIVEBEAM_GATEWAY_BIND:-0.0.0.0:8080}"
export HIVEBEAM_GATEWAY_DATA_DIR="${HIVEBEAM_GATEWAY_DATA_DIR:-$HOME/.config/hivebeam/gateway}"
